## Summary

What does this PR change, and why?

## Related spec / issue

- Spec: `spec/done/...` or `spec/future/...` (if this follows the
  project's spec-first workflow — see [`CONTRIBUTING.md`](../CONTRIBUTING.md))
- Closes #

## Checklist

- [ ] `npx tsc -b --noEmit` passes
- [ ] `npm run lint` passes
- [ ] Verified against the real running app (not just typecheck/lint) —
      describe how below
- [ ] New user-facing strings go through `t()` (i18n), not hardcoded literals
- [ ] No hardcoded secrets, and no unrelated/speculative changes bundled in

## Verification

How did you confirm this actually works? (Screenshots, exported file
contents, console output, steps you clicked through, etc.)

## Additional context

Anything a reviewer should know that isn't obvious from the diff.
